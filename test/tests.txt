/// Automated tests to implement in the future ///

// is* //
d: {a, -b, c:na()}

isMarker(d->a)  // True
isMarker(d->b)  // False
isMarker(d->c)  // False

isRemove(d->a)  // False
isRemove(d->b)  // True
isRemove(d->c)  // False

isNA(d->a)  // False
isNA(d->b)  // False
isNA(d->c)  // True

// keep //
keep([0, 1, 2, 3], 1)       // [1]
keep([0, 1, 2, 3], [3, 2])  // [2, 3]
keep([0, 1, 2, 3], 4)       // []

keep({a, b, c}, "a")         // {a}
keep({a, b, c}, ["c", "b"])  // {b, c}
keep({a, b, c}, "d")         // {}